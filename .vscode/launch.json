{
  "version": "0.2.0",
  "configurations": [

      {
          "type": "lldb",
          "request": "launch",
          "name": "Lldb debug",
          "program": "${fileDirname}/${fileBasenameNoExtension}",
          "args": [
          "${fileDirname}/*.o",
          "-o",],
          "cwd": "${workspaceRoot}"
      }
  ]
}